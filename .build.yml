image: archlinux
secrets:
- b64778ba-0b5e-49b2-9f75-0d9a79d34fbf
packages:
  - go
sources:
  - https://github.com/go-ap/processing
environment:
  GO111MODULE: 'on'
tasks:
  - setup: |
      cd processing && go mod download
  - tests: |
      cd processing && make test
  - coverage: |
      set -a +x
      cd processing && make coverage
      GIT_SHA=$(git rev-parse --verify HEAD)
      GIT_BRANCH=$(git name-rev --name-only HEAD)
      source ~/.code-cov.sh
      curl -X POST \
          --data-binary @processing.coverprofile \
           -H 'Accept: application/json' \
          "https://codecov.io/upload/v2?commit=${GIT_SHA}&token=${PROCESSING_TOKEN}&branch=${GIT_BRANCH}&service=custom"
